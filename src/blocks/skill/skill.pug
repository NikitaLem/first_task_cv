mixin skill({title, val, maxVal})
    .skill
        p.skill__name= title
        .skill__value
            - for(var i = 0; i < val; i++)
                .value__circle.value__circle_full
            - for(var j = 0; j < (maxVal - val); j++)
                .value__circle